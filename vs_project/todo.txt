статус бар / тулбар

свойства окна

контролы дл€ управлени€ расположенем контролов

переписать property list с использованием twc

масштаб группы объектов

применение аыравнивател€ и т.д. до€ окон

интерфейсы в project.c от проектов


tofix:
выделение рамкой после ctrl
z-order при копировании + TODO

ѕјћя“№ “≈„≈“ (чуть чуть)!



Ѕудущее:
===================

кросс-платформенность
    окна
	кодогенератор
	структура object
	применение свойств
	вывод сообщений
	рисование, то что необходимо
	! кросс-генераци€ кода !
	
редактор кода
возможность выбирать используемую GUI либу из имеющихс€ (дл€ каждой либы реализовать интерфейс)

минимальный framework (dll), использование TWC в real-time произвольной программы:
 каждый  сможет по своему желанию (если автор не против) редактировать интерфейс под себ€, заранее (файлы проектов TWCP) и/или в real-time (dll), по сути IDE внутри любой программы
 если автор против, то можно будет шифровать ту часть output-файла, в которой содержитс€ интрефейс TWC
     + шифрование структур TWC на лету
     + шифрование всех данных на лету (в паре с даже далеко не дописанным r-protect будет гг изи)
 все струкруты совместимы с таковыми в IDE
 можно собрать разные версии, в которых
      просто создаютс€ контролы
     + измен€ютс€ свойства, API дл€:
              только установка новых значений (без слежени€ за ними) (light-верси€)
              + изменение свойств real-time с полным контролем их изменени€
     + загрузка TWCP ( API TWCP<->IL)
     + редактор GUI из IDE ( + кодогенератор ( IL<->.C ) )
     + хуй знает когда редактор кода из IDE + перекомпил€ци€ на лету (MMAXVIMUM OWNEDHARD-верси€)
     
    конвертер x86-x64